## Summary

<!--
What does this PR do and why? A few bullet points is enough.
-->

## Test plan

- [ ] `bundle exec rake` passes locally (rspec + rubocop)
- [ ] If this touches Rails-version-specific behavior: `bundle exec appraisal rake` passes across the supported Rails matrix
